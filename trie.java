class Trie(){

void trietable(){
int[] list = new int[4];
                list[0] =152 ;
                list[1] = 5;
                list[2] = 596;
                list[3] = 63;
// sort permet de trier les entier 
                Arrays.sort(list);
// afficher les resultat sur le console 
                for(int i = 0; i < list.length; i++)
                        System.out.println(list[i]);
}
}
